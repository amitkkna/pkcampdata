{
  "name": "pkpptreports",
  "version": "1.0.0",
  "description": "Internal Campaign Reporting Generator for Advertisement Agency",
  "scripts": {
  "dev": "npm run dev:client",
  "dev:client": "cd client && npm run dev",
  "build": "npm run build:client",
  "build:client": "cd client && npm run build",
  "install:all": "npm install && cd client && npm install"
  },
  "keywords": ["campaign", "reporting", "pdf", "powerpoint", "agency"],
  "author": "Your Agency",
  "license": "MIT",
  "devDependencies": {
  
  }
}
